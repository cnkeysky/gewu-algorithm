# Fixtures

Fixtures exercise public schemas and domain behavior. They are not automatically official content.

- `algorithm-units/valid/` contains schema-valid design fixtures.
- Future `algorithm-units/invalid/` fixtures should each document the validation error they are expected to produce.

The BFS fixture remains a draft. Its presence demonstrates representability, not reviewed learning quality or validated code.
