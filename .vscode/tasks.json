{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "gewu-vscode: build",
      "type": "shell",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}/editors/vscode"
      },
      "problemMatcher": "$tsc",
      "group": "build"
    }
  ]
}
